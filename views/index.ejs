<style>
  .pagination-button{
    display: flex;
    justify-content: space-between;
    
  }
</style>
<div class="author">
  <h1 class="author__heading">Hi, I am Ehthisham.</h1>
  <p class="author__body">Web developer and content creator.</p>
</div>


<%- include('./partials/carousel.ejs', { data: data }) %>
  

<section class="articles">
  <h2 class="articles__heading">Latest Posts</h2>

  <ul class="article-ul">
    
    <% data.forEach(post => { %>

      <% if (post.title && post.title.trim() !== "") { %>
        <li>
          <a href="/post/<%= post._id  %>">
            <span>
              <%= post.title %>
            </span>
            <span class="article-list__date">
              <%= post.createdAt.toDateString() %>
            </span>
          </a>
        </li>
        <% } %>
        <% }) %>
      
  </ul>
  
  <div class="pagination-button">
    <div>
      <% if (previousPage !== null) { %>
        <a href="/?page=<%= previousPage %>" class="pagination">&lt; Previous</a>
      <% } %>
    </div>
    <div>
      <% if (nextPage !== null) { %>
        <a href="/?page=<%= nextPage %>" class="pagination">Next &gt;</a>
      <% } %>
    </div>
  </div>

</section>
<div style="display: flex; justify-content: space-between;">
  <div></div>
  <a href="/admin">
    <button class="login-btn">
    Admin
  </button>
  </a>
</div>